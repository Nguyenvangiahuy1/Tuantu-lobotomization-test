if not game:IsLoaded() then game.Loaded:Wait() end --------------- START

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui") or Players.LocalPlayer.PlayerGui

local partthatdestroystheguialreadyexist = ReplicatedStorage:FindFirstChild("DESTROYLOBOTOMIZER")
if partthatdestroystheguialreadyexist then
    partthatdestroystheguialreadyexist:Destroy()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "LOBOTOMIZER PRE-EXECUTE",
        Text = "LOBOTOMIZER IS ALREADY EXECUTED! DESTROYING OLD GUI...",
        Duration = 5
    })
end

local player = Players.LocalPlayer

local assets = ReplicatedStorage.Assets
local remotes = assets.RemoteEvents
local abnormalities = workspace.Abnormalities
local units = workspace.Units

---------------------------------------------------------- FUNCS
local function work(tablet, workType)
    if not tablet or not workType or (workType ~= "Attachment" and workType ~= "Insight" and workType ~= "Instinct" and workType ~= "Repression") then 
        notif("What are you working with?", "Work Error")
        return
    end
    if not tablet:IsA("BasePart") or tablet.Name ~= "WorkTablet" then return end
    if tablet.Parent.IsWorking.Value == true then 
        notif("Someone is working at that abnormality.", "Work Error") 
        return
    end

    remotes.WorkEvent:FireServer(tablet, workType)
end

local function extract(abnormality, item)
    if not abnormality then 
        notif("What abnormality are you extracting from?", "Extraction Error") 
        return 
    end
    if not item or (item ~= "Suit" and item ~= "Weapon") then 
        notif("What are you extracting?", "Extraction Error") 
        return 
    end

    remotes.ExtractEquipment:FireServer(abnormality, item)
    
    local info = safeFind(abnormality, {"InfoTablet","AbnormalityGui","Frame","EquipmentInfo"})
    local itemName

    if item == "Suit" then
        itemName = safeFind(info, {"SuitEquipment","EquipmentName"})
    elseif item == "Weapon" then
        itemName = safeFind(info, {"WeaponEquipment","EquipmentName"})
    end

    if itemName then
        if itemName.Text == "[Name]" then
            notif("Abnormality does not have a "..item, "Extraction Error: Got Nothing")
            return
        end

        notif("Extracted "..itemName.Text, "Extraction Request Success")
    end
end

local function getRoot(char)
    return char:FindFirstChild("HumanoidRootPart")
end

local function getHuman(char)
    return char:FindFirstChildOfClass("Humanoid")
end

function safeFind(root, path)
    local current = root
    for _, name in ipairs(path) do
        if not current then return nil end
        current = current:FindFirstChild(name)
    end
    return current
end

-- NEW HACK FUNCTIONS
local function godMode(enabled)
    if not player.Character then return end
    local humanoid = getHuman(player.Character)
    if humanoid then
        if enabled then
            humanoid.MaxHealth = math.huge
            humanoid.Health = math.huge
        else
            humanoid.MaxHealth = 100
            humanoid.Health = 100
        end
    end
end

local function infiniteStamina(enabled)
    local char = player.Character
    if not char then return end
    local stamina = safeFind(char, {"Stats", "Stamina"})
    if stamina then
        if enabled then
            stamina.Value = math.huge
        else
            stamina.Value = 100
        end
    end
end

local function speedMultiplier(multiplier)
    local humanoid = getHuman(player.Character)
    if humanoid then
        humanoid.WalkSpeed = 16 * multiplier
    end
end

local function autoWork()
    for _, abnormality in pairs(abnormalities:GetChildren()) do
        if abnormality:IsA("Model") then
            local workTablet = abnormality:FindFirstChild("WorkTablet")
            if workTablet then
                work(workTablet, selectedType)
                task.wait(0.5)
            end
        end
    end
end

local function autoExtract(itemType)
    for _, abnormality in pairs(abnormalities:GetChildren()) do
        if abnormality:IsA("Model") then
            extract(abnormality, itemType)
            task.wait(0.3)
        end
    end
end

local function teleportToMouse()
    local mouse = player:GetMouse()
    local root = getRoot(player.Character)
    if root and mouse.Hit then
        root.CFrame = mouse.Hit + Vector3.new(0, 3, 0)
    end
end

---------------------------------------------------------- VARS
local enemyEsp = false
local godModeEnabled = false
local infiniteStaminaEnabled = false
local autoWorkEnabled = false
local autoExtractEnabled = false
local currentSpeedMultiplier = 1

------------------------------------------------------------------------------ RAYFIELD SECTION
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "Lobotomizer",
   LoadingTitle = "LOBOTOMIZER LOADING...",
   LoadingSubtitle = "by Ali [UPDATED]",
   ShowText = "L",
   Theme = "Default",

   ToggleUIKeybind = "K",

   DisableRayfieldPrompts = true,
   DisableBuildWarnings = true,
})

function notif(txt, tit, dur)
    Rayfield:Notify({
        Content = txt,
        Title = tit or "Notification",
        Duration = dur or 5
    })
end

local abTab = Window:CreateTab("Abnormalities")
local visualTab = Window:CreateTab("Visual")
local charTab = Window:CreateTab("Character")
local hackTab = Window:CreateTab("Hacks")
local settingsTab = Window:CreateTab("Settings")

local destroying = false

--------------------------------------------------------------------------- WORK SECTION
local selectedAbnormalities = {}
local selectedType = "Selected Type"
local foundAbnormalities = {}
local waa
local tpta
local peekallinfo
local peeking = false
local abnormalityPeeking = selectedAbnormalities
local modified = {}
local restoreGuis

local abdd = abTab:CreateDropdown({
    Name = "Select Abnormalities",
    Options = {},
    CurrentOption = nil,
    MultipleOptions = true,
    Callback = function(option)
        selectedAbnormalities = {}
        for _, n in option do
            table.insert(selectedAbnormalities, foundAbnormalities[n])
        end
        local first = selectedAbnormalities[1]
        if first then
            tpta:Set("Teleport to the First Selected Abnormality ("..first.Name..")")
        end
    end
})

abTab:CreateDropdown({
    Name = "Work Type",
    Options = {
        "Instinct",
        "Insight",
        "Attachment",
        "Repression"
    },
    CurrentOption = nil,
    MultipleOptions = false,
    Callback = function(option)
        selectedType = option[1]
        waa:Set("Work at Selected Abnormalities with "..selectedType)
    end
})

abTab:CreateButton({
    Name = "Find Abnormalities",
    Callback = function()
        abdd:Set({})
        selectedAbnormalities = {}
        local a = {}
        foundAbnormalities = {}

        for _, model in abnormalities:GetChildren() do
            table.insert(a, model.Name)
            foundAbnormalities[model.Name] = model
        end

        abdd:Refresh(a)
        notif("Found "..#a.." abnormalities", "Search Complete")
    end
})

abTab:CreateSection("Actions")

waa = abTab:CreateButton({
    Name = "Work at Selected Abnormalities with Selected Type",
    Callback = function()
        if selectedType == nil or selectedType == "Selected Type" then
            notif("Please select a work type first", "Error")
            return
        end

        for _, abnormality in pairs(selectedAbnormalities) do
            work(abnormality.WorkTablet, selectedType)
            task.wait()
        end
    end
})

abTab:CreateButton({
    Name = "Extract Weapon from Selected Abnormalities",
    Callback = function()
        for _, abnormality in pairs(selectedAbnormalities) do
            extract(abnormality, "Weapon")
        end
    end
})

abTab:CreateButton({
    Name = "Extract Suit from Selected Abnormalities",
    Callback = function()
        for _, abnormality in pairs(selectedAbnormalities) do
            extract(abnormality, "Suit")
        end
    end
})

tpta = abTab:CreateButton({
    Name = "Teleport to the First Selected Abnormality (nil)",
    Callback = function()
        local abnormality = selectedAbnormalities[1]
        local root = getRoot(player.Character)

        if root and abnormality then
            root.CFrame = abnormality.LeavePosition.CFrame
            notif("Teleported to "..abnormality.Name, "Success")
        else
            notif("No abnormality selected", "Error")
        end
    end
})

peekallinfo = abTab:CreateButton({
    Name = "Peek Selected Abnormality's Locked Info [OFF]",
    Callback = function()
        peeking = not peeking

        if not peeking then
            restoreGuis()
            abnormalityPeeking = nil
        end
    end
})

local function setGui(gui, props)
    if not gui or not gui.Visible then
        return
    end

    if not modified[gui] then
        modified[gui] = {}
    end

    for property, value in pairs(props) do
        if modified[gui][property] == nil then
            modified[gui][property] = gui[property]
        end

        gui[property] = value
    end
end

restoreGuis = function()
    for gui, properties in pairs(modified) do
        if gui and gui.Parent then
            for property, original in pairs(properties) do
                gui[property] = original
            end
        end
    end

    table.clear(modified)
end

local function peekAbnormality(selectedAbnormality)
    local infoGui = safeFind(selectedAbnormality, {"InfoTablet","AbnormalityGui"})
    local frame = infoGui and infoGui:FindFirstChild("Frame")

    local classified = infoGui and safeFind(infoGui, {"ClassifiedInfo","LockedFrame"})
    local cbutton = classified and classified:FindFirstChild("LockedButton")

    local equipment = frame and frame:FindFirstChild("EquipmentInfo")

    local weapon = equipment and safeFind(equipment, {
        "WeaponEquipment",
        "RequirerResearchLabel"
    })

    local suit = equipment and safeFind(equipment, {
        "SuitEquipment",
        "RequirerResearchLabel"
    })

    local gift = equipment and safeFind(equipment, {
        "GiftInfo",
        "GiftEquipment",
        "RequirerResearchLabel"
    })

    local management = frame and safeFind(frame, {
        "ManagementInfo",
        "ScrollingFrame"
    })

    local research = frame and frame:FindFirstChild("ResearchInfo")

    setGui(classified, {
        BackgroundTransparency = 0.5
    })

    setGui(cbutton, {
        BackgroundTransparency = 0.5,
        TextTransparency = 0.5
    })

    for _, gui in {weapon, suit, gift} do
        setGui(gui, {
            BackgroundTransparency = 0.25,
            TextTransparency = 0.5
        })
    end

    if management then
        for _, mt in management:GetChildren() do
            if mt:IsA("TextLabel")
            and mt.Name:find("MT")
            and mt:FindFirstChild("LockedFrame") then

                local lf = mt.LockedFrame
                local lb = lf:FindFirstChild("LockedButton")

                setGui(lf, {
                    BackgroundTransparency = 0.5
                })

                setGui(lb, {
                    BackgroundTransparency = 0.5,
                    TextTransparency = 0.5
                })
            end
        end
    end

    if research then
        for _, rt in research:GetChildren() do
            if rt.Name == "Attachment"
            or rt.Name == "Insight"
            or rt.Name == "Instinct"
            or rt.Name == "Repression" then

                local lf = rt:FindFirstChild("LockedFrame")
                local lb = lf and lf:FindFirstChild("LockedButton")

                setGui(lf, {
                    BackgroundTransparency = 0.5
                })

                setGui(lb, {
                    BackgroundTransparency = 0.5,
                    TextTransparency = 0.5
                })
            end
        end
    end
end

RunService:BindToRenderStep("PEEK", Enum.RenderPriority.Last.Value + 1, function()
    restoreGuis()

    if not peeking then
        peekallinfo:Set("Peek All Selected Abnormalities' Locked Info [OFF]")
        return
    end

    peekallinfo:Set("Peek All Selected Abnormalities' Locked Info [ON]")

    for _, abnormality in pairs(selectedAbnormalities) do
        peekAbnormality(abnormality)
    end
end)

--------------------------------------------------------------------------- ESP SECTION
visualTab:CreateSection("Abnormalities")
local espObj = {}
local eet = visualTab:CreateToggle({
    Name = "Enemy ESP",
    CurrentValue = enemyEsp,
    Callback = function(v)
        enemyEsp = v

        if #espObj > 0 then
            for _, o in espObj do
                if o and o.Parent then
                    o:Destroy()
                end
            end
            espObj = {}
        end
    end
})

local lastRefresh = tick()
local refreshRate = 5
RunService:BindToRenderStep("ESP", Enum.RenderPriority.Camera.Value + 2, function(...)
    if enemyEsp then
        if tick() - lastRefresh >= refreshRate then
            lastRefresh = tick()
            
            if #espObj > 0 then
                for _, o in espObj do
                    if o and o.Parent then
                        o:Destroy()
                    end
                end
            end
            espObj = {}
        end

        for _, unit in units:GetChildren() do
            if unit.Name == "Clerk" or Players:GetPlayerFromCharacter(unit) or not unit:IsA("Model") or unit:FindFirstChild("BillboardGui") or not unit:FindFirstChild("RiskLevel") then 
                continue
            end
            
            local unitRoot = getRoot(unit)
            local unitHuman = getHuman(unit)

            if not unitRoot or not unitHuman then continue end

            local hl = Instance.new("Highlight")
            hl.Adornee = unit
            hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            hl.FillColor = Color3.fromRGB(255, 0, 0)
            hl.FillTransparency = 0.5
            hl.OutlineTransparency = 0
            hl.Parent = CoreGui
            table.insert(espObj, hl)

            local billboard = Instance.new("BillboardGui")
            billboard.Adornee = unit.HumanoidRootPart
            billboard.AlwaysOnTop = true
            billboard.Size = UDim2.fromOffset(150, 40)
            
            local nameText = Instance.new("TextLabel")
            nameText.Name = "EnemyName"
            nameText.Text = unit.Name
            nameText.TextScaled = true
            nameText.TextColor3 = Color3.new(1,1,1)
            nameText.BackgroundTransparency = 1
            nameText.Size = UDim2.fromScale(1, 0.33)
            nameText.Position = UDim2.fromScale(0, 0)
            nameText.Parent = billboard
            
            local distanceText = Instance.new("TextLabel")
            distanceText.Name = "Distance"
            distanceText.Text = "Distance: 0"
            distanceText.TextScaled = true
            distanceText.TextColor3 = Color3.new(1,1,1)
            distanceText.BackgroundTransparency = 1
            distanceText.Size = UDim2.fromScale(1, 0.33)
            distanceText.Position = UDim2.fromScale(0, 0.33)
            distanceText.Parent = billboard
            
            local hpText = Instance.new("TextLabel")
            hpText.Name = "HP"
            hpText.Text = "Health: 100"
            hpText.TextScaled = true
            hpText.TextColor3 = Color3.new(1,1,1)
            hpText.BackgroundTransparency = 1
            hpText.Size = UDim2.fromScale(1, 0.33)
            hpText.Position = UDim2.fromScale(0, 0.67)
            hpText.Parent = billboard

            billboard.Parent = unit
            table.insert(espObj, billboard)

            task.spawn(function()
                while unit and unitRoot and unit.Parent and billboard and billboard.Parent do
                    if destroying then break end

                    local plrChar = player.Character
                    local root = plrChar and getRoot(plrChar)
                    unitHuman = getHuman(unit)

                    if root then
                        local dist = (unitRoot.Position - root.Position).Magnitude
                        distanceText.Text = "Distance: "..tostring(math.round(dist*100)/100)
                    end

                    if unitHuman then
                        hpText.Text = "Health: "..math.round(unitHuman.Health)
                    end

                    task.wait(.5)
                end 
            end)
        end
    end
end)

---------------------------------------------------------------------------------------- CHARACTER SECTION
charTab:CreateSection("Humanoid")
local setWS = false
local setJP = false
local currentWS = getHuman(player.Character) and getHuman(player.Character).WalkSpeed or 16
local currentJP = getHuman(player.Character) and (getHuman(player.Character).JumpPower or getHuman(player.Character).JumpHeight) or 50

charTab:CreateToggle({
    Name = "Set Walkspeed",
    CurrentValue = setWS,
    Callback = function(v)
        setWS = v
    end
})

charTab:CreateToggle({
    Name = "Set Jumppower",
    CurrentValue = setJP,
    Callback = function(v)
        setJP = v
    end
})

charTab:CreateSlider({
    Name = "Walkspeed",
    Range = {10, 200},
    Increment = 2,
    CurrentValue = currentWS,
    Callback = function(v)
        currentWS = v
    end
})

charTab:CreateSlider({
    Name = "Jumppower",
    Range = {1, 100},
    Increment = 1,
    CurrentValue = currentJP,
    Callback = function(v)
        currentJP = v
    end
})

charTab:CreateDivider()
charTab:CreateSection("Teleportation")

charTab:CreateButton({
    Name = "Teleport to Mouse",
    Callback = function()
        teleportToMouse()
        notif("Teleported to mouse position", "Success")
    end
})

RunService:BindToRenderStep("SETHUMANOID", Enum.RenderPriority.Character.Value + 2, function(...)
    if setWS then
        local humanoid = getHuman(player.Character)
        if humanoid then
            humanoid.WalkSpeed = currentWS
        end
    end
    if setJP then
        local humanoid = getHuman(player.Character)
        if humanoid then
            if humanoid.UseJumpPower then
                humanoid.JumpPower = currentJP
            else
                humanoid.JumpHeight = currentJP
            end
        end
    end
end)

--------------------------------------------------------------------------- HACKS SECTION
hackTab:CreateSection("Combat Hacks")

hackTab:CreateToggle({
    Name = "God Mode",
    CurrentValue = godModeEnabled,
    Callback = function(v)
        godModeEnabled = v
        godMode(v)
        notif("God Mode: ".. (v and "ON" or "OFF"), "Hack Status")
    end
})

hackTab:CreateToggle({
    Name = "Infinite Stamina",
    CurrentValue = infiniteStaminaEnabled,
    Callback = function(v)
        infiniteStaminaEnabled = v
        infiniteStamina(v)
        notif("Infinite Stamina: ".. (v and "ON" or "OFF"), "Hack Status")
    end
})

hackTab:CreateSlider({
    Name = "Speed Multiplier",
    Range = {1, 10},
    Increment = 0.5,
    CurrentValue = 1,
    Callback = function(v)
        currentSpeedMultiplier = v
        speedMultiplier(v)
    end
})

hackTab:CreateSection("Auto Functions")

hackTab:CreateToggle({
    Name = "Auto Work",
    CurrentValue = autoWorkEnabled,
    Callback = function(v)
        autoWorkEnabled = v
        notif("Auto Work: ".. (v and "ON" or "OFF"), "Hack Status")
    end
})

hackTab:CreateToggle({
    Name = "Auto Extract Weapon",
    CurrentValue = false,
    Callback = function(v)
        if v then
            autoExtract("Weapon")
            notif("Auto extracting weapons", "Extraction")
        end
    end
})

hackTab:CreateToggle({
    Name = "Auto Extract Suit",
    CurrentValue = false,
    Callback = function(v)
        if v then
            autoExtract("Suit")
            notif("Auto extracting suits", "Extraction")
        end
    end
})

hackTab:CreateSection("Vision")
hackTab:CreateButton({
    Name = "No Fog",
    Callback = function()
        game.Lighting.Ambient = Color3.fromRGB(200, 200, 200)
        game.Lighting.ClockTime = 12
        notif("Fog disabled", "Success")
    end
})

-- Auto work loop
RunService:BindToRenderStep("AUTOWORK", Enum.RenderPriority.Character.Value + 1, function()
    if autoWorkEnabled and selectedType ~= "Selected Type" then
        autoWork()
    end
    
    if godModeEnabled and player.Character then
        local humanoid = getHuman(player.Character)
        if humanoid and humanoid.Health < humanoid.MaxHealth then
            humanoid.Health = humanoid.MaxHealth
        end
    end
end)

--------------------------------------------------------------------------- SETTINGS SECTION
function destroyGui()
    if destroying then return end

    notif("Destroying...")
    destroying = true
    print("start destroying assets")

    RunService:UnbindFromRenderStep("ESP")
    for _, o in ipairs(espObj or {}) do
        if o and o.Parent then
            o:Destroy()
        end
    end
    espObj = nil
    print("unbinded esp and cleaned esp objects")

    RunService:UnbindFromRenderStep("SETHUMANOID")
    print("unbinded sethumanoid")

    RunService:UnbindFromRenderStep("PEEK")
    print("unbinded peek")

    RunService:UnbindFromRenderStep("AUTOWORK")
    print("unbinded autowork")

    restoreGuis()
    print("restored peeked guis")

    foundAbnormalities = nil
    modified = nil
    print("cleaned all tables")

    if ReplicatedStorage:FindFirstChild("DESTROYLOBOTOMIZER") then
        ReplicatedStorage.DESTROYLOBOTOMIZER:Destroy()
    end
    print("partthatdestroystheguiwhenitisdestroyed has been destroyed without re-destroying the gui")

    print("done, now destroying rayfield...")
    task.wait(.5)
    Rayfield:Destroy()
end

settingsTab:CreateSection("GUI")
settingsTab:CreateButton({
    Name = "Destroy GUI",
    Callback = function()
        destroyGui()
    end
})

settingsTab:CreateSection("Info")
settingsTab:CreateLabel("Tuantu's Lobotomization Script")
settingsTab:CreateLabel("Version: 2.0 - Updated with Hacks")
settingsTab:CreateLabel("Created by: Ali [Updated]")

------------------------------------------------------------ THE END
local partthatdestroystheguiwhenitisdestroyed = Instance.new("Part")
partthatdestroystheguiwhenitisdestroyed.Name = "DESTROYLOBOTOMIZER"
partthatdestroystheguiwhenitisdestroyed.Parent = ReplicatedStorage
partthatdestroystheguiwhenitisdestroyed.Destroying:Once(destroyGui)

notif("Lobotomizer Executed!", "Lobotomizer GUI v2.0")
